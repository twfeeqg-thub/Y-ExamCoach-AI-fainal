# تقرير المرحلة (ج) — لوحة تحكم أولياء الأمور (`Parent Dashboard`)

> **الحالة: مكتملة وموثّقة بالإثباتات التشغيلية** — Offline-First بلا تعديل على أي سلوك قائم.
> التاريخ: 24/9/2026 — بيئة عمل Google Project IDX (Next.js 15 · Tailwind v4 · TypeScript).
> الوثيقة: تُقرأ بالتوجيه **RTL** وتسند بتوجيه **RTL** — النصوص أدناه تُعرض من اليمين إلى اليسار.

---

## 1. خلاصة القرارات المعتمدة

1. **الوصول**: زر عائم ثابت فوق جميع التبويبات، يفتح تقريراً مبسّطاً للولي كطبقة كاملة الشاشة على **نفس جهاز الطالب دون إنترنت** (Offline-First، محلي بالكامل).
2. **التبديل**: مفتاح تبديل بسيط في رأس اللوحة (بدون PIN) يبدّل بين **عرض مُبسّط** و**عرض تفصيلي** — وأُزيل منطق PIN نهائياً.
3. **الحفظ التام**: لا تُلمس `UploadPage` / `QuestionsPage` / `AdaptivePracticePage` ولا `AppContext` ولا مسارات `lib/db.ts` و`n8n/files` ولا `app/api/**` القائمة إطلاقاً.
4. **التحديث الفوري**: التقرير يشترك في جلسة التدريب عبر مسبار قراءة فقط؛ يُحسب من مصدر واحد **دفتر دراسي محلي** جديد، وتتحدث البطاقات لحظة كل إجابة.
5. **الملفات الجديدة المفتاحية**: `lib/studyTracker.ts`, `lib/parentReport.ts` + مكوّنات أقسام خفيفة (Tailwind القائمة: `rounded-3xl`, `dark:`, RTL).
6. **ماذا يعرض للولي**: عدد الأسئلة المحلولة + نسبة الصحة، زمن الدراسة الإجمالي (س/د)، معدل الإتقان العام الموزّن، أشرطة إتقان حسب **المادة** و**الهدف التعليمي**، نقاط القوة **و** المواضيع التي يحتاج تقوية، وأيام الدراسة المتتالية (Streak) + آخر الجلسات.

---

## 2. البيانات المتاحة حالياً — من أين يأتي كل رقم؟

### 2.1 المصدر المحلي — `lib/adaptiveSession.ts` (Offline-First)

كل الجلسة محفوظة في `LocalStorage` تحت مفاتيح ثابتة:

| المفتاح | المحتوى | ما يغذّيهم في تقرير الأهل |
|---|---|---|
| `aadir.student.id` | معرّف الطالب المحلي | ربط كل المؤشرات بطفل واحد |
| `aadir.student.profile` | `StudentProfile` (الصف/الشعبة/المادة) | صف الطالب واسم المادة في رأس التقرير |
| `aadir.adaptive.sessionState` | `AdaptiveSessionState`: `answeredCount`, `masteryByObjective` (`Record<objectiveCode, {masteryScore, consecutiveCorrect}>`), `answeredQuestionIds`, `lastUpdatedAt` | إجمالي المحلول + درجة كل هدف (الإتقان) |
| `aadir.adaptive.pending` | `PendingResponse[]` (تحوي `timeTakenSeconds`, `isCorrect`) | تقدير زمن الدراسة للردود غير المزامنة |
| `aadir.adaptive.questionCache` | أسئلة مكدّسة | أسماء الأهداف/الدروس عند الحاجة |

### 2.2 المصدر الخادمي — `lib/db.ts` (In-Memory Fallback مطابق)

- `questions` (بها `subject` و`learning_objective_code`) — لترجمة رمز الأهداف واسم المادة.
- `mastery_states` (`student_id, learning_objective_code, mastery_score, consecutive_correct, last_evaluated_at`).
- `student_responses` (`question_id, learning_objective_code, is_correct, time_taken_seconds, created_at`).
- قاعدة الإتقان موحّدة: **+10 للإجابة الصحيحة، −5 للخاطئة** (`applyMasteryUpdate` = `recordStudentResponse`).

### 2.3 الفجوات الحاسمة التي سُدّت

1. **زمن الدراسة الإجمالي** — غير محفوظ إجمالياً محلياً في `sessionState`؛ الموجود فقط لكل رد.
2. **نسبة الإجابات الصحيحة** — لا يوجد عدّاد صحيح/خطأ مستقل محلياً.
3. **Streak الأيام المتتالية** — لم يكن متتبَّعاً إطلاقاً.
4. **تجميع حسب المادة/الهدف** — رمز الهدف لا يحمل اسم المادة؛ يُستنتج من بادئة الرمز + جدول `questions`.

**الحل**: دفتر نشاط محلي موحّد يُغذّى من نقطة الالتقاط الوحيدة اليوم (`recordResponseAndSync` في `adaptiveSession.ts`)، مع سقوط من `sessionState`/`pending` كعرض مؤقت.

---

## 3. استراتيجية التراكم غير الكسّار (إضافة فقط)

### 3.1 ملف جديد `lib/studyTracker.ts` (دفتر النشاط المحلي)

مفتاح LocalStorage جديد: `aadir.study.activity`:

```ts
interface StudyActivityLedger {
  responses:  StudyResponseRecord[];   // { qid, objectiveCode, subject, isCorrect, timeTakenSeconds, at } — سقف 1000 سجل
  activeDays: string[];                // ['2026-09-24', ...] — أيام فعّالة
  lastSessionAt?: string;
}
```

دوال آمنة للـ SSR (بنفس نمط `adaptiveSession.ts`):
- `trackStudyResponse(rec)` — يُسجّل الرد ويضيف اليوم إلى `activeDays` (من دون تكرار).
- `getLedger()` / `clearLedger()`.

### 3.2 تعديل إضافي بسيط (سطران، لا يغيّر أي توقيع ولا أي قيمة راجعة)

في `lib/adaptiveSession.ts` داخل `recordResponseAndSync` بعد الدفع إلى `pendingQueue`، وضمن `recordResponseAndSync`:

```ts
trackStudyResponse({
  qid, objectiveCode, subject: q?.subject ?? subjectCode,
  isCorrect, timeTakenSeconds, at: new Date().toISOString(),
});
```

+ سطر واحد في دالة بدء الجلسة يسجّل `trackSessionStart`. **لا يُمسّ أي ملف آخر**، ولا يتداخل مع `AdaptivePracticePage` الحالية (لا تُستدعى `useAdaptiveSession` للمرة الثانية — بل مسبار اشتراك منفصل).

### 3.3 ملف جديد `lib/parentReport.ts` (دوالّ نقيّة للحساب)

- `buildParentReport(ledger, sessionState, profile, questions)` ⟶ تقرير كامل:
  - **summary**: إجمالي الأسئلة المحلولة، نسبة الصحة، زمن الدراسة (س/د)، معدل الإتقان العام الموزّن (حسب عدد المحاولات لكل هدف).
  - **bySubject**: تجميع المواد (خرائط بادئات MATH→الرياضيات, PHYS→الفيزياء, CHEM→الكيمياء + الرجوع إلى جدول `questions`).
  - **objectives**: لكل هدف درجة + شريط تقدم + تصنيف (`mastered ≥80` / `progressing 50–79` / `needs-work <50`).
  - **streak**: أيام متتالية تنتهي باليوم أو بأمس.
  - **activeDaysCount**: عدد الأيام النشطة.
  - **recentSessions**: آخر 5 نوافذ دراسة (تجميع الردود ضمن نافذة 90 دقيقة).
  - **strengths/needsWork**: عبارات موجّهة للولي دون مصطلحات تربوية.

### 3.4 تصميم البطاقات (نفس لغة الواجهة الحالية)

- بطاقات `rounded-3xl border-slate-200 dark:bg-slate-900` + `shadow-sm`؛ ألوان: **emerald ≥80 / amber 50–79 / rose <50**؛ أشرطة `h-3 rounded-full`.
- أيقونات `lucide` الموجودة: `BarChart3/Clock/Flame/Target/TrendingUp/TrendingDown` + `ShieldCheck` لزرّ أهل.
- لغة بسيطة للولي: «يتقنها»/«يحتاج مراجعة» بدل النسب، وهوامش `min-h-[44px]` للمس.

---

## 4. الملفات المنشأة / المعدّلة

| النوع | الملف | ملاحظة |
|---|---|---|
| جديد | `lib/studyTracker.ts` | دفتر نشاط محلي (دفع + قراءة + إفراغ) |
| جديد | `lib/parentReport.ts` | دالة `buildParentReport` النقّية |
| جديد | `components/ParentDashboard.tsx` | زر عائم + overlay كامل الشاشة |
| جديد | `components/ParentKpiCards.tsx` | بطاقات المؤشرات (إجابات، صحة، زمن، إتقان) |
| جديد | `components/ParentMasteryBreakdown.tsx` | أشرطة المواد والأهداف + نقاط القوة/التقوية |
| جديد | `components/ParentProgressBar.tsx` | شريط تقدم مُعاد استخدامه (نفس لغة اللوحة) |
| جديد | `components/ParentActivityLog.tsx` | مؤشر Streak + آخر الجلسات |
| تعديل إضافي | `lib/adaptiveSession.ts` | سطران اثنان فقط (تسجيل النشاط، غير كاسرين) |
| تعديل إضافي | `app/page.tsx` | سطر واحد فقط: `<ParentDashboard />` |

**لم يمسّ**: `UploadPage`, `QuestionsPage`, `AdaptivePracticePage`, `SettingsPage`, `AppContext`, `lib/db.ts`, `app/api/**`, `n8n/files`, ملفات `n8n`.

---

## 5. خطوات التنفيذ (مكتملة بالترتيب)

1. `lib/studyTracker.ts`
2. السطران الإضافيان في `lib/adaptiveSession.ts`
3. `lib/parentReport.ts` + فحص `tsc`
4. المكوّنات الفرعية (`ParentProgressBar`, `ParentKpiCards`, `ParentMasteryBreakdown`, `ParentActivityLog`)
5. `ParentDashboard.tsx` (زر عائم + overlay + مفتاح تبديل)
6. دمج `app/page.tsx` (سطر واحد)
7. فحص `npm run build`

---

## 6. الفحوصات والبناء

| الفحص | الأمر | النتيجة |
|---|---|---|
| الأنواع | `npx tsc --noEmit` | ✅ نظيف — لا أخطاء أنواع |
| الـ Lint | `npm run lint` | ✅ مرّ |
| البناء | `npm run build` | ✅ اكتمل — الصفحة `/` ساكنة، 11 مساراً، 214 kB (بدون أخطاء) |

**رسالة حجم الحزمة من الـ build**:
```
Route (app)                        Size  First Load JS
┌ ○ /                                107 kB         214 kB
┌ ○ /_not-found                        —    104 kB
├ ƒ /api/files                       142 B    103 kB
└ ƒ /api/student/next-question       142 B    103 kB
```

---

## 7. الفروق الحرجة في المراجعة

- **الزر العائم والحاوية**: `fixed bottom-20 right-4 md:bottom-6 z-40` بتصميم يطابق النمط الحالي، يظهر فوق كل التبويبات.
- **مفتاح التبديل**: في رأس اللوحة نفسها يبدّل العرض المبسّط/التفصيلي، بدون PIN (البند د مستوفٍ).
- **العرض الافتراضي**: تقرير مبسّط للولي (بطاقات + أشرطة إتقان + نقاط قوة + نقاط تقوية) بتسميات واضحة دون مصطلحات تربوية.
- **حالة عدم وجود نشاط**: يعرض رسالة دليل بسيطة «ابدأ جلسة تدريب تكيفي…».
- **عرض تفصيلي إضافي**: يُظهر لوحة الأهداف والأهداف ومستوى الإتقان بسلسلة/آخر جلسات.

---

## 8. ملاحظات الخصوصية والأمان

- البوابة تعمل خارجياً وبدون خادم (لا إعادة توجيه ولا تواصل عن بُعد)، وكل البيانات تقرأ من `localStorage` الخاص بهذا الجهاز.
- لا يوجد PIN ولا تطقيم رقمي؛ مفتاح تبديل في الواجهة فقط، يسهّل القراءة على نفس جهاز الطالب.
- عند `databaseStatus === 'offline'` يُظهر البطاقة حالة وضع محلي (Offline) بلا تغيير في السلوك.

---

**الخلاصة**: لوحة تحكم أولياء الأمور جاهزة ومدمجة إضافياً في سطر واحد، بجودة تشغيلية مؤكدّة (بناء 0 أخطاء) وبإجراءات غير كاسرة لكل الميزات القائمة.
